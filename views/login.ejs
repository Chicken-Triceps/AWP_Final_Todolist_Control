<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>로그인 - 일정 관리 서버</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="login-wrapper">
        
        <div class="login-card">
            <h1 class="login-title">Login</h1>
            
            <form id="login-form" action="/auth/login" method="post">
                <input type="text" name="email" class="login-input" placeholder="ID (Email)" required>
                <input type="password" name="password" class="login-input" placeholder="PASSWORD" required>
                
                <button type="submit" class="btn-black">Login</button>
            </form>
            
            <a href="/auth/kakao" class="btn-kakao">
                카카오톡으로 로그인
            </a>

            <% if (loginError) { %>
                <p class="error-message" style="margin-top: 20px; color: red;">
                    <%= loginError %>
                </p>
            <% } %>
            
            <div class="login-links">
                아직 계정이 없으신가요? <a href="/join">Sign Up</a>
                <br><br>
                <a href="/" style="text-decoration: none; color: #999;">← 메인으로 돌아가기</a>
            </div>
        </div>

    </div>
</body>
</html>